@model ExercisesViewModel
@*<script>
        $("#next").click(function(e) {
        e.preventDefault();
        $.ajax({
            type: "POST",
            url: "/home/page/",
            data: {
                id: $(this).val(), // < note use of 'this' here
                access_token: $("#access_token").val()
            },
            success: function(result) {
                alert('ok');
            },
            error: function(result) {
                alert('error');
            }
        });
    });
    </script>*@
<style>
    .fa {
        color: #ffc107;
    }
</style>



<h2>Exercises</h2>
<div class="row">
    <ul class="nav nav-tabs">
        @{var first = true;
            var categoryId = 0;}
        @foreach (Category ca in Model.Categories.results)
        {
            if (first)
            {
                <li class="nav-item active">
                    <a class="nav-link active" data-toggle="tab" aria-expanded="true" href="#@ca.name">@ca.name</a>
                </li>
                first = false;
                categoryId = ca.id;
            }
            else
            {
                <li class="nav-item">
                    <a class="nav-link" data-toggle="tab" href="#@ca.name">@ca.name</a>
                </li>

            }
        }
    </ul>
    <div class="tab-content">
        @foreach (Category ca in Model.Categories.results)
        {
            if (ca.id == categoryId)
            {
                
             @:<div class="tab-pane active" id="@ca.name">
            }
            else
            {
                
            @:<div class="tab-pane" id="@ca.name">

            }
                @foreach (Exercise ex in Model.Exercises.results)
                {
                    if (ex.category == ca.id)
                    {
                        <ul class="list-group media-list media-list-stream">
                            <ul class="list-group">
                                <li class="list-group-item">
                                        <div class="row">
                                            <div class="col-md-2">
                                                @if (ex.imageURL != null)
                                                {
                                                    <img style="max-width:100px; max-height:100px" class="card-img" src="@ex.imageURL" alt="@ex.name" />
                                                }
                                                else
                                                {
                                                    <img style="max-width:100px; max-height:100px" class="card-img" src="~/images/dumbbellSVG.png" alt="@ex.name" />
                                                }
                                            </div>
                                            <div class="col-md-9">
                                                <h5 class="font-weight-bold">
                                                    @ex.name
                                                </h5>
                                                <p>
                                                    @Html.Raw(ex.description)
                                                </p>
                                            </div>
                                            <div class="col-md-1">
                                                <span data-toggle="tooltip" data-placement="top" title="More info about this Exercise">@Html.ActionLink("", "ExerciseDetail", "Home", new { id = ex.id }, new { @class = "fa fa-info-circle program" })</span>
                                            </div>
                                        </div>
                                </li>
                            </ul>
                        </ul>
                    }
                }
            @:</div>
        }
    </div>
</div>

    @*@Html.ActionLink("Next page", "NavExercises", "Home", new { query = Model.Exercises.next }, new { @class = "btn btn-primary" })*@


